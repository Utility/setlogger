


export const DEFAULT_SCHEMA = {
  id: 'string',
  rev: 'string',
  type: 'array',
  items: 'array',
  children: 'array',
  data: 'object',
  strict: false // set to true for strict type checking on nodes
}
